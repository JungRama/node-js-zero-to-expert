extends ../layouts/main-layout.pug
block content
    if products.length > 0
        p List of Product
            each product in products
                li #{product.title}
    else
        p No Product